#include <bits/stdc++.h>
#include <utility>
#include <vector>

using namespace std;

#define ll long long
#define ld long double


unordered_map<ll,vector<pair<ll,ll>>> graph;

int main() {
    int n,m,q,s;
    while (cin >> n >> m >> q >> s) {
        if (n==0 && m==0 && q==0 && s=0) break;
    }
    for (int i=0;i < 0;i++) {
        int u,v,w;
        cin >> u >> v >> w;
    }
    return 0;
}

